import { atom } from 'recoil'

export const OpenForgotPasswordFormState = atom({
  key: 'OpenForgotPasswordFormState',
  default: false
})

export const OpenRegisterFormState = atom({
  key: 'OpenRegisterFormState',
  default: false
})
